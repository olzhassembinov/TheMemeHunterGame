## Setup

1. Copy `Assets/Resources/.../MapboxConfiguration.txt.example` to
   `MapboxConfiguration.txt` in the same folder.
2. Replace the placeholder with your own Mapbox secret access token
   (get one at mapbox.com → Account → Tokens).
3. `MapboxConfiguration.txt` is gitignored — never commit it.
